'''
✅ What you can import in Python:
1. 🌟 Built-in modules (come with Python automatically)
python
Copy code
import math        # for math functions
import random      # for random numbers
import datetime    # for dates & time
import os          # for operating system tasks
import sys         # system-specific stuff
import time        # time delays, etc.
2. 🧰 Standard library tools (no need to install)
python
Copy code
import json        # work with JSON
import csv         # read/write CSV files
import statistics  # mean, median, etc.
import re          # regular expressions
import shutil      # file operations (copy, move)
3. 🚀 External libraries (you install them using pip)
python
Copy code
import numpy       # powerful math with arrays
import pandas      # data analysis
import matplotlib  # plotting graphs
import requests    # make web requests
import flask       # web app
import pygame      # build games
🛠️ To install them, use: pip install library_name

🎯 Syntax for importing:
python
Copy code
import math               # imports whole module
from math import sqrt     # imports just `sqrt` function
import math as m          # gives it a short name (alias)
🔍 Pro Tip: See all built-in modules
Run this in Python:

python
Copy code
help('modules')
It will show all available built-in modules you can import.

'''